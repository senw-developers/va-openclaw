<!-- SENW→VA parity-port execution dossier. Compiled 2026-08-05 by a 20-agent audit+verification sweep
     (13 cohorts, high confidence on all verified) against pins va 79369129ce / senw 9571e4775f.
     Payload byte-verified 65/65. Baseline gates recorded. Secret-scanned against both trees: clean.
     Companions: senw-to-va-parity-playbook.md (the authority, in ~/Desktop/va-parity-handover/),
     upstream-bump-2026-08-{drift-map,seam-dossier}.md (Job 2, the later upstream merge). -->

# SENW → VA Parity-Port Execution Dossier

Repo: `senw-developers/va-openclaw`, branch `develop` @ `79369129ce` (clean). Compiled 2026-08-05 from 13 verified audit cohorts + byte-verification. Path conventions: bare paths = our tree @ `79369129ce`; `pin:<path>` = nabu-senw @ `9571e4775f`; `sN:hunk@<path>` = handover patch; `handover:<path>` = the va-parity-handover pack (playbook = `handover:senw-to-va-parity-playbook.md`). **Security rule: every token value visible in either tree is burned; this dossier names keys and lengths only, never values.**

---

## 0. Authenticity & validity

| Fact                   | Value                                                                                                                       | Proof                                                    |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| Our tip                | `79369129ce` == `origin/develop`, tree clean, **0 unpushed commits**                                                        | `git rev-parse`, `git log origin/develop..develop` empty |
| senw pin               | `9571e4775f`, fetched locally as `refs/senw-probe/develop` (tip `675c26d997`); pin is an ancestor of their tip              | local fetch, ancestry check                              |
| Drift since pin        | 4 commits, **docs-only** under `nabu-integration/` — no code drift                                                          | probe log                                                |
| Shared base / ancestor | `fc6400ede3` (OpenClaw 2026.6.2) / `e8770727e4`                                                                             | playbook Pins table                                      |
| Byte-verify            | 8 patches applied in a scratch tree: **files=65, mismatches=0, applyfail=0** — every patched file byte-identical to the pin | scratchpad `parity-verify.txt`                           |
| Re-diff                | 49 senw-only commits, 393 differing files — **matches playbook Appendix A/B exactly**                                       | `parity-verify.txt`                                      |
| Pack vs living doc     | Pack playbook is **identical** to the living doc at their tip                                                               | `parity-verify.txt`                                      |

**Verdict: the payload is authenticated.** The patches are a faithful re-bundling of the pin; §12 validity (playbook:517-526) **holds** — pins match, re-diff counts match, drift is docs-only. Standing condition: if either fork merges upstream before the port completes, the playbook is void for conflicted surfaces and our own merge method (`nabu-integration/docs/upstream-merge-playbook.md`, memo `5e4394cabed9`) governs there.

Two consequences of the patch **re-bundling** (verified, not defects): (a) the s3 patch content is 100% commit `cdff7f09c4` — `3119b8e552`'s deltas ride s1/s4/s7; (b) the `chat-file-refs.ts` MEDIA-strip and `ResolveIdentityOpts` deltas the playbook narrates under S3 physically ride `s5-ingress-identity.patch`, which relocates the playbook's "S3→S5 fileRefs-empty window" (see §1.3).

---

## 1. Settled decisions

**1.1 G1 — Tenancy = COMPOSITION (operator-confirmed 2026-08-05; playbook "Settled decisions" §1, §5 table, playbook:24-30, 353-366).** We KEEP multi-org (env-derived `OPENCLAW_ORGANIZATION_ID`, one gateway stack per org, `x-organization-id` headers, per-org skill tokens) and ADD senw's per-user dimension (numeric `userId` from session/body). Identity tuple = `(organizationId, userId)`. Everywhere senw's D22/D19 deleted org, we re-add it (§3 ledger). `parseSessionOwner` stays userId-only; org never enters core code paths — composition happens at plugin header-builder and env layers only. senw's D19 single-tenant collapse (`handover:docs/SPEC.md:565-607`) is consciously NOT adopted.

**1.2 Scope exclusions (playbook §7, :404-420).** Do not inherit: Simon Says persona/rebrand/scrub commits, the knowledge corpus, committed credentials (Pattern A — every token visible in either tree is burned; our own `nabu-integration/spawn-seed/.env` is gate G4), senw deployment facts (base URLs, `agent-<userId>` naming, model-router-disabled seed state), and junk (stale T5 test assertions, unused D17 workspace consts, resurrected package.json scripts). The pin additionally ships its own live secrets (`pin:nabu-integration/spawn-seed/.env:52`, 1Password service-account value, `<redacted>`) — never bulk-checkout their spawn-seed.

**1.3 Stage-order rule + do-not-ship window (playbook:91-96).** Core userId threading (S3–S5) and the nabu-files numeric gate (S6a) land together or in that order — gate without threading refuses every upload (trap T6). The playbook's literal "S3→S5 fileRefs EMPTY" transient described senw's own commit sequence; under **this patch bundling** the fail-closed `batchResolve` ships inside s5 together with all its caller grafts, so the window is benign **iff s5 is applied atomically**. The real do-not-ship states are: **(a) any partial-s5 build** (fail-closed gate live without a caller graft ⇒ silently empty fileRefs), **(b) S6a deployed before S3–S5 threading**, and **(c) any prod deploy between S3 and S5** as a belt-and-braces rule. Tag `parity/s<N>` after each green stage.

---

## 2. Stage-by-stage execution plan

### S0 — Preflight (no changes)

- Branch `feat/senw-parity` off develop; tag `parity/s0-baseline`.
- Assumptions confirmed on our tree: 4 hook-plugin manifests lack `activation`; no manifest has `contracts.tools`; `extensions/nabu-media-upload/openclaw.plugin.json` has `onStartup:false`; `src/sessions/session-key-shapes.ts` absent; `parseSessionOwner` absent from `src/sessions/session-key-utils.ts` (zero grep hits).
- Baseline **recorded** — see §8. All three tsgo lanes + all three plugin-SDK gates PASS; 8 known-red tests in 4 files, each mapped to a pre-existing fork defect (§8 table). Docker build/boot-line before-state: **PENDING** (node_modules was mid-install; docker lane owns it — record before S1).
- Recommended pre-port fixes (independent of the payload; shrink the known-red list): dm.pair data-loss (#9), `wrapReadFileForHttpUrls` revert (#3/Q7), prompt-cache boundary decision (#2/Q8), G4 secret rotation/untracking — see §5.

### S1 — Manifests (`patches/s1-manifests.patch`, 5 files)

Fixes a **live defect today**, not just a prerequisite: our nabu-email tool registrations are refused right now — the registry gate is form-agnostic and our manifest declares no `contracts.tools` (`src/plugins/registry.ts:605-613` pushes an error diagnostic and returns without registering; `src/plugins/tool-contracts.ts:4-8` has no fallback). Also unbricks nabu-files/nabu-gateway/nabu-google-workspace activation (seam-dossier `nabu-integration/docs/upstream-bump-2026-08-seam-dossier.md:226,599`) and flips nabu-media-upload.

Beyond the playbook's "~10 lines", the patch carries: (a) D16/D22 **description rewrites** on nabu-email/nabu-files (org→"platform token"/x-user-id-only wording, "senw-core"/"SPEC.md D16" references, an "x-channel is no longer sent" clause that never applied to us); (b) a NEW `apiToken` config property + sensitive uiHint on nabu-gateway (correct forward-declaration for s6b under `additionalProperties:false`).

- **ADAPT:** rewrite the description hunks to composed G1 wording in the same commit as the s6 header work (§3 ledger), or apply verbatim and fix at S6 — never leave descriptions claiming userId-only scoping while runtime sends org (`extensions/nabu-files/src/upload.ts:167-168` sends org until s6). Replace "senw-core"/"SPEC.md" refs (port `handover:docs/SPEC.md` into `nabu-integration/docs/` or rewrite). Decide nabu-model-router activation — un-activated at the pin too (`pin:extensions/nabu-model-router/openclaw.plugin.json`); senw runs it disabled; if we enable it, also see watchlist #34/#35 (§5).
- **Gate:** docker build; boot line lists the plugins; log `[nabu-files] media uploader + resolver registered`. **Runtime proof only** (trap T3 — `openclaw plugins list` shows contract plugins "disabled" while they fire).
- **Interaction:** seed `plugins.allow` still blocks nabu-media-upload (`src/plugins/loader.ts:1945`, `src/plugins/config-activation-shared.ts:227-235`) until the S8 seed edit — expected.

### S2 — SDK + owner identity (`patches/s2-sdk-identity.patch`, 7 files)

Additive/inert: `userId?` on `MediaUploadInput`/`MediaResolverInput`/`AgentToolResultMiddlewareContext`; NEW `src/sessions/session-key-shapes.ts`; `parseSessionOwner` v2 grafted into `src/sessions/session-key-utils.ts` (exactly 2 hunks; upstream's `normalizeSessionKeyPreservingOpaquePeerIds` and all other exports untouched); NEW 185-LOC contract test + type-fence test.

- **Manual (not in patch):** (1) `pnpm plugin-sdk:api:gen` then `pnpm plugin-sdk:api:check` — 3 types on public SDK entrypoints change (`scripts/lib/plugin-sdk-entrypoints.json:88,89,117`); pin's checked-in baseline is byte-identical to ours _despite_ its type changes (stale senw regen or member-insensitive generator — `src/plugin-sdk/api-baseline.ts:335-342`), so expect possible drift and regen, never hand-copy. Our baseline check passes today (§8). (2) Restore media-uploader/media-resolver rows in `docs/plugins/sdk-overview.md` (absent in BOTH trees; `docs/plugins/sdk-subpaths.md:323-326` still documents them). (3) **CI guard**: new fork-owned `scripts/check-fork-graft-exports.mjs` (modeled on `scripts/check-plugin-sdk-subpath-exports.mjs`) asserting `parseSessionOwner`/`SessionOwner` exported and `session-key-shapes.ts` exists, wired as a package.json lint script — a Vitest-only guard dies in the same take-theirs sweep that once deleted the export (bucket-b authorization-bypass incident). (4) Recommended: dual-load wiring guard for the media-uploader singleton (#8, §5) while these files are open.
- **Gate:** 3 tsgo lanes; `pnpm test src/sessions/session-key-utils.test.ts`; `pnpm plugin-sdk:check-exports && pnpm lint:plugins:plugin-sdk-subpaths-exported && pnpm plugin-sdk:api:check`.
- **Expected:** all green. The `openresponses-user`/`openai-user` branch is dead code until s5 (no producer exists yet) — unit tests are the only proof at this stage.
- **Future-merge flag (Job 2):** upstream rewrites `src/plugins/agent-tool-result-middleware-types.ts` (removes harness fields our :7-10,:26,:44-45 still have; adds `matcher`/`Scope` — `nabu-integration/docs/upstream-bump-2026-08-drift-map.md:151-159`); the `userId` line must be re-added onto upstream's new shape at merge time. `session-key-shapes.ts` is fork-only by design (low conflict surface).

### S3 — Detached-media Files-API delivery (`patches/s3-detached-media.patch`, 10 files) — the memo-047 debt

Content is 100% commit `cdff7f09c4` (as resolved through senw's merge `d9ac82fd4a`). **Hard dependency: S2 first** — the uploader call passes `userId` as a direct property (tsgo excess-property failure without S2) and `server-session-events.ts` imports `parseSessionOwner`.

What lands: producer-side chokepoint `uploadCompletionAttachmentsInPlace` (`pin:src/agents/tools/media-generate-background-shared.ts:309-397`; numeric `/^\d+$/` requester gate, 10s per-attachment `Promise.race`, fail-open, rewrite to signed URLs, mint `fileIds`) + synthetic `media-completion` assistant card via `appendExactAssistantMessageToSessionTranscript` (exists at `src/config/sessions/transcript.ts:256-264`; idempotency `${toolName}:${taskId}:${status}:media-card`); `requesterUserId` threading through the 3 generate tools; **rewrite of our #29 graft** in `src/gateway/server-session-events.ts:192-221` (async post-projection IIFE → sync pre-projection enrichment with owner userId; restores ordering, kills the unhandled-rejection path); wake-bubble hiding widened (now also `agent_harness_task` and any user text containing the internal-context marker); `media-completion` added to **both** T4 registries; reply instruction changes "media path" → "media **URL**" MEDIA lines (`pin:src/agents/tools/media-generate-background-shared.ts:420-424`) — the producer half of the s5 MEDIA-strip story.

- **ADAPT/decide:** (1) channel-native numeric senderIds (Telegram/WhatsApp) pass the `/^\d+$/` gate and would upload under a channel-native id as backend owner — ingress must map to real backend userIds or leave `requesterSenderId` unset for non-web surfaces (ties to B5-1). (2) Interim state: `enrichMessageWithFileRefs` receives `userId` via conditional spread that our pre-s5 signature ignores (compiles; live fileRefs stay org-scoped mid-stack) — have the compile lane confirm. (3) Optional: fix dossier #30 (SSE history branch discards enrichment — **unfixed at the pin**, `pin:src/gateway/sessions-history-http.ts:218-233`); optional: consolidate the third inline transcript-only duplicate (`src/agents/embedded-agent-subscribe.handlers.messages.ts:49-56`, not extended by senw — harmless today, live trap later).
- **Known behaviors to accept:** everything is fail-open+silent (post-S1 the uploader exists, so the residual dead-code risk is only #8 dual-load); the 10s race does **not** abort the underlying upload — it can complete server-side after the race is lost (orphaned backend file, quota/attribution relevance under G1); enrichment latency now sits inline on every `session.message` broadcast.
- **Gate:** 3 tsgo lanes; T4 check (`grep -rln "media-completion" src/ | wc -l` ≥ 2). Full runtime smoke (image_generate → card with `details.nabuFileIds` → hidden wake bubble → no raw `MEDIA:` in web display → signed URL on channel) completes only after S5/S6a+backend; do not deploy before S5.

### S4 — userId threading + middleware ctx + browser delivery (`patches/s4-threading-browser.patch`, 4 files)

- `src/agents/embedded-agent-runner/extensions.ts` (+`resolveMediaOwnerUserId`, parseSessionOwner-first, numeric senderId fallback) + `run/attempt.ts` call-site (+2; anchor verified at `src/agents/embedded-agent-runner/run/attempt.ts:2111-2117`; both params exist on the type) + `extensions/nabu-media-upload/src/middleware.ts` `(event, ctx)` (already the runner contract — `src/agents/harness/tool-result-middleware.ts:445`; only the ctx field comes from S2).
- **G5 gate on the browser hunk only** (`extensions/browser/src/browser-tool.ts` — separable; exclude if undecided). Corrected framing for the decision: our tree **already auto-delivers raw screenshots** on the no-vision and vision-failure paths (`extensions/browser/src/browser-tool.ts:857-872` → `details.media` via `imageResult`); the S4 hunk extends that to vision-success. Native channels auto-send `details.media` through core delivery **regardless of nabu-media-upload**; the plugin is needed only for the files-api/signed-URL (web) path. With the uploader unconfigured, the local path rides the broken #3 outbound read.
- Intentional caller difference to note in-code: the compaction path (`src/agents/embedded-agent-runner/compact.ts:1162`) is un-threaded in the pin too — compaction runs never carry ctx.userId (low impact; post-S6a any media there uploads refused).
- **Gate:** 3 tsgo lanes; `pnpm test extensions/nabu-media-upload` (baseline: 3 green). No test deltas ride s4 — decide whether to add middleware userId-passthrough coverage.

### S5 — HTTP-ingress identity re-grafts (`patches/s5-ingress-identity.patch`, 6 files) — **apply atomically**

Apply MINUS the `src/gateway/server-runtime-config.ts` `canvasHostEnabled` hunk (**G10 = DROP**: zero code consumers in either tree — `pin:src/gateway/server-runtime-config.ts:44,138,207` only; the live canvas runtime is the `extensions/canvas` plugin which owns its own enablement via plugin config + `OPENCLAW_SKIP_CANVAS_HOST` and never reads this field; porting adds a dead config surface against repo policy).

What lands: `senderIsOwner` re-grafts on `openai-http.ts` + `openresponses-http.ts` (helper exists: `src/gateway/http-auth-utils.ts:250-262`); `ownerUserId = payload.user?.trim()` threaded into `uploadInboundImage` (fail-closed `if (!identity.userId) return null;`) and both `resolveTurnFileRefs` sites; `parseSessionOwner`-based owner into `chat.history`/`chat.startup` (+ bundled `stripEnvelopeFromMessages`) and `sessions-history-http.ts`; `chat-file-refs.ts` `ResolveIdentityOpts` + fail-closed `batchResolve` + the MEDIA-line strip.

- **Prerequisite:** B5-1 (ingress sends numeric `user`) **before prod deploy** — else all inbound-image persistence/turn fileRefs stop while model vision keeps working (masked loss). Also keep `user` and any explicit `x-openclaw-session-key` consistent at the ingress: uploads are owned by body `user` while enrichment derives owner from the session key — a split writes as X and reads as Y (or never) once per-user isolation lands backend-side.
- **Record at this stage (do not inherit silently):** ledger entry **D-SEC-1** — the optional `x-openclaw-session-key` header lets any operator/bearer-scope caller act as an arbitrary userId toward files-api (`pin:nabu-integration/tech-debt.md:78-79` RED, unfixed both forks; our tree has **no** ledger file yet — create `nabu-integration/tech-debt.md`). Under G1 the fix must bind `(orgId, userId, sessionKey)`; options HMAC-bind / operator-JWT allowed-userIds / refuse-header (recommended: JWT + HMAC layered; va-core-nest issues the JWT).
- **Behavior changes to sign off:** bearer callers flip `senderIsOwner` false→true (unlocks owner-gated command auth — `src/agents/agent-tools.ts:530-531`); owner-less sessions (`agent:<id>:main`, groups, cron, subagent, acp, anonymous HTTP) get permanently empty fileRefs (operator decision for our webchat, §10); non-numeric `user` passes core truthiness until S6a+B6-1; MEDIA-strip removes visible pointers for non-generated media without fileRefs (web display only).
- **Optional adapt:** consolidate the now-four inline `user→senderId/ownerUserId` derivations into one helper (Q4/#11) — do it during the graft, diff-verify vs pin after. Zero test deltas ride s5; consider regression tests for the two fail-closed gates.
- **Gate:** 3 tsgo lanes; POST `/v1/responses` with numeric `user` + input_image → upload keyed to user; `response.completed` carries turn fileRefs (full backend proof completes at S6a+B6-1). Re-run the take-va-as-is greps (bucket-b list: `src/agents/command/types.ts:94,102`, `src/gateway/server-methods.ts:327-328`, `src/infra/outbound/message-action-params.ts:187`, `src/agents/system-prompt.test.ts:177`).

### S6 — Plugin wire contracts (`patches/s6-plugin-contracts.patch`, 12 files) — org+user composition

Land code first; **enable per deployment** only when B6-1/2/3 are live (nabu-files degrades to local-path fallback while `apiToken` unset — which then rides broken #3; nabu-gateway ingest has no fallback).

- **S6a nabu-files:** numeric `/^\d+$/` upload gate, truthy resolve gate (asymmetry noted, §10), `u:<userId>:` sha1-capped idempotency (≤128), `ResolveHttpError` + envelope parse (`pin:extensions/nabu-files/src/resolve.ts:21-29,129-137`), `x-user-id` headers, index.ts passthrough. **ADAPT: re-add org at all 8 sites** (§3) + test env stubs/assertions. Observability note: pin's `ResolveHttpError.message` drops the response body our old code embedded (`extensions/nabu-files/src/resolve.ts:113` vs `pin:…:136`) — keep the body in the message or add a log. Optional: unify the timeout budget (Q16 — chokepoint/middleware 10s vs uploader 30s×3 ladder; pass an AbortSignal so raced-out uploads actually abort).
- **S6b nabu-gateway:** `apiToken` read + `x-skill-token` on usage ingest (schema half already landed in s1). Org payload untouched by senw — no re-add needed. T8 preserved structurally (per-event `getLivePluginConfig`); Q2 caveat: `loadConfig()` returns the pinned runtime snapshot, refreshed only via gateway `config.patch` → `src/gateway/server-methods/config-write-flow.ts:224` → `src/config/runtime-snapshot.ts:294-343` — rotation works **iff** NestJS rotates via the RPC, never by direct file writes (§10).
- **S6c nabu-email (D16):** context-factory registration, trusted `context.agentId` injected post-spread (hostile params overwritten), refusal without it, zero identity fields in schema. **SKIP the typebox import hunk** (trap T2 — keep `import { Type } from "typebox"`; API-proven with our typebox 1.1.39; pin's root also retains bare typebox at `pin:package.json:1934`, so no dep change, no T11). **ADD `x-organization-id`** (§3; recommend fail-closed, §10).
- **S6d nabu-google-workspace:** apply the `index.ts` comment cleanup unconditionally; **HOLD `SKILL.md` + `nabu-google.tool.ts:156` string pending G2** — against a drive.file-only backend the dual-scope SKILL actively misleads the agent. Org composition already intact (untouched by s6).
- **S6e nabu-model-router:** test-only mock hardening — apply as-is.
- **Follow-up commit (same PR, not byte-parity):** migrate the 5 remaining deprecated `loadConfig()` sites (`extensions/nabu-email/index.ts:22`, `extensions/nabu-files/src/config.ts:14`, `extensions/nabu-gateway/index.ts:62,75`, `extensions/nabu-google-workspace/src/config.ts:22`) to `api.runtime.config.current()` — `removeAfter 2026-07-27` has passed (`src/plugins/compat/registry.ts:880-901`); **not a drop-in** (returns `DeepReadonly<OpenClawConfig>`; test stubs mock loadConfig). Preserve per-call re-read (T8).
- **Gate:** 3 tsgo; `pnpm test extensions/nabu-files` — **13 green only after** the two T5 fixes + org env stubs (§7-T5 exact edits); runtime smokes vs backend dev: skill-upload 201 with **BOTH** headers; cross-user resolve → per-entry `{fileId,error:"NOT_FOUND"}`; ingest 200 with token / 401 without; email send → agent-mapped mailbox or `SMTP_CONFIGURATION_NOT_CONFIGURED`.

### S7 — nabu-1password v3 (`patches/s7-1password.patch`, 15 files: 12 plugin + 3 root-skill deletions)

Replaces our v1 org shim (process.env injection — the cross-user confused-deputy leak) with v3: `nabu_1password` read-only tool, per-invocation broker fetch, token confined to the `op` **child** env only (`pin:extensions/nabu-1password/src/op-runner.ts:114-118`), no cache/refresh RPC, `api.runtime.config.current()`. Retires seam-dossier items :196/:197/:198/O12/O13 and watchlist #32 entirely. Deletes `skills/1password/` (3 files — SKILL.md + 2 references; re-delete on every future upstream merge; keep the seed's `skills.entries.1password.enabled:false` as a resurrection guard).

- **ADAPT:** org re-add in `fetchUserOpToken` (fail-closed; §3) + interface comment + manifest `apiToken` description reword (v1's "Per-organization skill bearer" at `extensions/nabu-1password/openclaw.plugin.json:13` was rewritten org-free at `pin:…:19` — restore per-org wording); verify the "Simon Says dashboard" 412 string against our branding; optionally add an org readiness check in `logBackendStatus` (`pin:extensions/nabu-1password/src/token.ts:125-133`) and a token.test.ts case locking the org header.
- **Backend coordination (hard):** B7 must serve `POST /api/v1/onepassword/access-token` (`{userId,channel}` + `x-skill-token` + `x-organization-id`) **and stop calling `nabu.onepassword.refresh`** — that RPC 404s the moment v3 lands, independent of enable state. Retire the v1 `/token` route; drop `refreshIntervalMs` from any `config.patch` payload (now schema-rejected under `additionalProperties:false`).
- **Deploy steps (manual):** seed entry exists (`plugins.allow` already lists nabu-1password) — flip `enabled:true` + `apiToken` → env SecretRef `{source:"env",provider:"default",id:"NABU_ONE_PASSWORD_SKILL_TOKEN"}` (nabu-files pattern) only after B7; add `nabu-1password` to `tools.alsoAllow` (profile-gated — `src/agents/tool-policy.ts:214`; our-tree extra the playbook misses); add `NABU_ONE_PASSWORD_SKILL_TOKEN` to spawn-seed `.env` + gateway compose environment (pure ADD in our tree — no `ONE_PASSWORD_ACCESS_TOKEN` exists anywhere to rename; do not import the pin's stale Dockerfile comment or their burned `.env:52` value). Update/banner-deprecate the v1 docs: `nabu-integration/docs/1password-nestjs-implementation.md` (also contains the v1 OP_SERVICE_ACCOUNT_TOKEN/org contract at :88-89,:630-644,:906-917) and `nabu-integration/result.md:50`.
- **Gate:** `pnpm test extensions/nabu-1password`; boot +1 plugin; ordered smoke: (1) channel-message → `nabu_1password` vault-list succeeds; (2) agent exec `printenv OP_SERVICE_ACCOUNT_TOKEN; echo probe-rc=$?` → no value, `probe-rc=1` (exec children inherit gateway env, so child absence proves gateway absence); (3) `grep -c '"OP_SERVICE_ACCOUNT_TOKEN"' src/infra/host-env-security-policy.json` = 0 (guards probe fidelity). Never use `/proc/<pid>/environ` (passes even under leaking v1). Dockerfile op bake already correct in our tree (`Dockerfile:303-343`; GPG-pinned, install :341-342, `op --version` :343) — untouched by s7.

### S8 — Build/deploy + hygiene (`patches/s8-build-deploy.patch`, 6 files + 4 manual G9 reverts + seed work)

- **Dockerfile — hand-merge, never verbatim** (the patch REGRESSES `93ee52f8e7`): keep our ENV budget (`Dockerfile:134-138`: 1800s/12 retries/1200s maxtimeout) + our `{…;} || echo non-fatal` wrapper (`Dockerfile:141-143`), inserting senw's `grep -vE` non-linux-prebuilt filter (from `8057a9aff0`, `pin:Dockerfile:158-159`) inside the wrapper before `xargs -r pnpm store add`; adopt the additive install-stage fetch flags (s8:hunk@Dockerfile:71-80) as-is; **exclude the qmd bake block** (`pin:Dockerfile:~404-421`, `ARG OPENCLAW_INSTALL_QMD` defaults **"1"** — G8=skip); keep our per-tenant 1Password comment wording (§3). Byte-parity intentionally breaks for this file — verify via docker build gate instead.
- **`extensions/memory-core/src/memory/qmd-manager.ts`:** exclude (G8=skip; cleanly separable — nothing else in s8 references qmd).
- **`.gitignore`:** adopt `nabu-integration/instances/*` rewrite + spawn-seed IDENTITY/USER un-ignores (inert for us today, harmless); SKIP `nabu-integration/knowledge/` (senw-local). The patch does **NOT** fix seam #12 — our anchored `/.env` (`.gitignore:3` vs upstream unanchored `fc6400ede3:.gitignore:3`) stays; that is the separate G4 action (rotate → `git rm --cached nabu-integration/spawn-seed/.env` → restore unanchored `.env` or add `nabu-integration/**/.env`) — do it BEFORE this hunk so the surface is edited once.
- **`.npmrc` (`verify-deps-before-run=false`) and `pnpm-workspace.yaml` (`sharp: true`):** SKIP both (playbook-optional; sharp exists in zero package.json in our repo; Docker sets the verify skip inline where needed).
- **`nabu-integration/scripts/validate-channel-isolation.mjs`:** take if G6=adopt; always pass an explicit config path (its `nabu-demo` default exits 2 here); wire into CI; sequence the default-deny config baseline first or it fails every existing instance.
- **G9 reverts (not in patch):** `git checkout main -- extensions/feishu/src/channel.ts extensions/github-copilot/models.ts extensions/irc/src/setup-core.ts extensions/zai/index.ts` (local `main` = `fc6400ede3`). Verified pure type-cast noise (+9/-18, zero behavior).
- **package.json:** do NOT port senw's 33 re-added root deps; add only what a gate proves missing (then T11 lockfile regen). If grafting pin scripts at all: only `moltbot:rpc` + the 3 `test:docker:*:codex` entries; the pin's `canon:*` and `stage:bundled-plugin-runtime-deps` scripts are **dangling** (backing files exist in neither tree — report back as a pin defect, §9).
- **Manual seed/deployment work (instructions, not patches — must actually be executed):**
  - G7 chain (adopt): migrate 5 seed surfaces off `openai-codex` (`nabu-integration/spawn-seed/.openclaw/openclaw.json:18,140,158`; `agents/main/agent/auth-profiles.json`; `agents/main/agent/models.json`) per commits `113c9b1457→cdf1822421→bbe423df94→d9519337e3→fae10e95c7`; **pin `agentRuntime.id:"openclaw"`** (LOAD-BEARING — without it the runtime spawns the codex CLI app-server and crashes, no fallback); ADD `"openai"` to `plugins.allow` + `entries` (our seed lacks it; the 5-commit chain doesn't cover it); never add `copyToAgents` to the declaration (schema-rejected); prefer `openclaw doctor --fix` in-container; untrack/regenerate `completions/*` (stale openai-codex ids; doctor won't fix them); fresh interactive re-auth required (committed OAuth material is expired + burned) + `--force-recreate` after.
  - `nabu-media-upload` → `plugins.allow` + `entries` (truly absent today).
  - Seed compose: add persistent `/home/node/.config/openclaw` volume (both services) + the three `NABU_{EMAIL,GOOGLE_WORKSPACE,ONE_PASSWORD}_SKILL_TOKEN` passthroughs (#13).
  - Re-sync vendored `nabu-integration/spawn-seed/docker-setup.sh` from upstream (#14); drop the pre-seeded bootstrap marker + regenerate seed + run doctor in provisioning (#16/Q13).
- **Gate:** full playbook §10 run (3 tsgo lanes; SDK trio; `DOCKER_BUILDKIT=1 docker build`; `--version` + `plugins list | grep -i nabu` boot; nabu-files 13 green; nabu-1password + nabu-media-upload green; validator if G6; runtime smoke set = memo-045 mirror).

---

## 3. Consolidated ADAPT ledger

Every post-patch edit, grouped by file. **Both directions verified**: the 8 s6/s7 code-site org removals below are the complete set of org deletions in the patches (pin-grep: zero org hits remain in nabu-files/nabu-1password; the three surviving pin org readers — `pin:extensions/nabu-google-workspace/src/token.ts:234`, `pin:extensions/nabu-google-workspace/src/tools/nabu-google.tool.ts:131`, `pin:extensions/nabu-gateway/index.ts:139` — need no action). Cross-checked against our 21 current org touchpoints (inventory: `nabu-integration/docs/upstream-bump-2026-08-seam-dossier.md:646` — keep it in sync).

**`extensions/nabu-files/src/upload.ts` (S6a — 4 org sites):**

1. After the numeric userId gate (`pin:…:62-66`), insert: `const organizationId = process.env.OPENCLAW_ORGANIZATION_ID; if (!organizationId) throw new Error(`${LOG_PREFIX} OPENCLAW_ORGANIZATION_ID not set`);` (keep both gates).
2. Add `organizationId,` beside `userId,` in the `postSkillUpload` call (`pin:…:93`).
3. Re-add `organizationId: string;` to `PostInput` (`pin:…:170-172`).
4. Re-add `"x-organization-id": input.organizationId,` beside `"x-user-id"` in headers (`pin:…:189`). Send BOTH.

**`extensions/nabu-files/src/resolve.ts` (S6a — 4 org sites):** mirror the four upload edits (env gate after the userId refusal `pin:…:44-46`; call-site `pin:…:58-63`; input type `pin:…:80-86`; header `pin:…:98`). Plus: keep the response body in `ResolveHttpError`'s message (or log it) — pin drops it (`pin:…:136` vs ours `:113`).

**`extensions/nabu-files/src/{upload,resolve}.test.ts` (S6a):** `beforeEach` set `process.env.OPENCLAW_ORGANIZATION_ID = "1"`, delete in `afterEach`; add `expect(headers["x-organization-id"]).toBe("1")` in the header tests. Plus the two T5 fixes (§7-T5). Without the env stub every transport test fails on the re-added gate.

**`extensions/nabu-email/index.ts` (S6c):** (a) SKIP the patch's first hunk — keep `import { Type } from "typebox"` (T2). (b) In `apiPost`, add org: read `OPENCLAW_ORGANIZATION_ID` and send `x-organization-id` beside `x-skill-token` (composition ADD — the header never existed here; B6-3 mandates it; recommend fail-closed, decide once — §10).

**`extensions/nabu-1password/src/token.ts` (S7):** at the top of `fetchUserOpToken` (after the `hasApiToken` guard, `pin:…:69-91`): env read + fail-closed throw (`"OPENCLAW_ORGANIZATION_ID not set; 1Password broker calls are org-scoped"`), then `"x-organization-id": organizationId,` immediately after `"x-skill-token"` in headers. Body stays `{userId, channel}`. Optional readiness check in `logBackendStatus` (`pin:…:125-133`); optional test case locking the header.

**`extensions/nabu-1password/src/nabu-1password.interface.ts` (S7):** reword the `OpTokenRequest` doc comment (pin :16 says "No organization id — single-tenant") to: org rides the `x-organization-id` header (env-derived, per-org stack — G1); no type change.

**`extensions/nabu-1password/openclaw.plugin.json` (S7):** restore per-org wording in the `apiToken` description (pin :19 dropped it).

**Manifest descriptions (S1↔S6 commit pairing):** rewrite nabu-email `apiToken` description and both nabu-files descriptions to composed wording ("requests carry x-organization-id (env-derived) AND x-user-id"); drop "senw-core"/"SPEC.md D16/D22" refs and the "x-channel is no longer sent" clause (never applied to us); land in the same commit as the s6 header work so description and runtime never disagree.

**`src/sessions/session-key-utils.ts` (S2):** explicit **no-op** — keep `SessionOwner = {userId}`; do NOT add an org field (org is env/header-level under G1).

**`src/gateway/server-runtime-config.ts` (S5):** DROP the `canvasHostEnabled` hunk entirely (G10).

**`src/gateway/chat-file-refs.ts` (S5):** apply the fail-closed `batchResolve` as-is; the alternative (fall through to org-scoped resolve when userId absent, so owner-less sessions keep fileRefs) is an operator decision (§10 Q5) — do not improvise it silently.

**`extensions/browser/src/browser-tool.ts` (S4):** exclude the hunk unless G5=adopt; if adopted, keep the in-code both-sides rationale comment.

**`extensions/nabu-google-workspace/skills/…/SKILL.md` + `src/tools/nabu-google.tool.ts` (S6d):** hold pending G2; `index.ts` comment hunk applies now.

**`Dockerfile` (S8):** hand-merge per S8 above (keep ours + insert filter; adopt install-stage flags; exclude qmd block; keep per-tenant 1P comment).

**`.gitignore` (S8):** adopt instances/\* + IDENTITY/USER hunks; skip `knowledge/`; G4 `.env` fix separately, first.

**`.npmrc` / `pnpm-workspace.yaml` (S8):** skip both hunks.

**Repo-wide manual items:** plugin-SDK baseline regen (S2); `docs/plugins/sdk-overview.md` rows (S2); `scripts/check-fork-graft-exports.mjs` CI guard (S2); Q2 `loadConfig→current()` follow-up (S6, 5 sites, not drop-in); seed/compose work + G7 chain + G9 reverts (S8, listed there); create `nabu-integration/tech-debt.md` ledger with D-SEC-1 (S5); port or reference `handover:docs/SPEC.md` (S1).

---

## 4. Decision gates

| Gate    | Question                                              | Recommendation                                                                                                                                                                                                                                                                                                                                                                     | Confidence | Needs operator                                                 | Blocks                                                     |
| ------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------- | ---------------------------------------------------------- |
| **G2**  | Drive dual-scope SKILL (drive.readonly + drive.file)? | Indeterminate from repo: our backend doc says Phase-1 `drive.file`-only (`nabu-integration/docs/google-workspace-nestjs-implementation.md:307-313`); **the pin's own copy of that doc still says drive.readonly is Phase-2** (`pin:…:311`) while its SKILL claims dual-scope — senw's artifacts contradict each other. Skip SKILL/tool-string until backend scopes are enumerated. | medium     | **YES** (verify granted scopes in va-core-nest/Google console) | s6d SKILL.md + tool.ts hunks only                          |
| **G3**  | Restore Control-UI origin guard?                      | ADOPT — boot-safe (seed `gateway.controlUi.allowedOrigins` non-empty at `nabu-integration/spawn-seed/.openclaw/openclaw.json:193-195`); un-reds 4 baseline tests; removes a merge hazard. Operator must supply real browser-facing origins per deployment (loopback-only list under `bind=lan` likely wrong at request time — `src/gateway/origin-check.ts:40,50`).                | high       | YES (prod origin list)                                         | none (independent; ride S8)                                |
| **G4**  | Committed spawn-seed secrets                          | Rotate + untrack + fix `.env` ignore rule vs scrub-from-history — **rotation was explicitly DECLINED 2026-07-22** (memo `23572d81e571`: old gateway token, device key, API keys in history, "treat as LIVE AND EXPOSED"); decision must be re-made. Pre-port.                                                                                                                      | high       | **YES**                                                        | precedes s8 `.gitignore` work; blocks nothing mechanically |
| **G5**  | Browser screenshot auto-delivery                      | Operator product call. Facts: browser is live on 3 seed config surfaces; our tree already auto-sends screenshots on no-vision/vision-failure paths — delta is vision-success only; native channels auto-send regardless of nabu-media-upload (plugin needed only for the web/signed-URL path); uploader-unconfigured fallback rides broken #3.                                     | high       | **YES**                                                        | one s4 hunk                                                |
| **G6**  | Default-deny channel baseline + validator             | ADOPT for seed/template (zero-risk: seed ships `channels:{}`); port validator with explicit path; live-instance rollout needs an operator audit (live configs not in repo — `nabu-integration/instances/` empty). D21 companion hardening is still DEFERRED on senw's side — baseline ≠ full hardening.                                                                            | medium     | YES (existing-instance audit; go-live timing)                  | s8 validator piece                                         |
| **G7**  | codex→openai migration chain                          | **ADOPT, near-unconditional** — we use ChatGPT OAuth (seed on `openai-codex/gpt-5.4`; committed tokens expired+burned). Operator action is execution-only (fresh re-auth, prod ChatGPT identity choice).                                                                                                                                                                           | high       | no (execution only)                                            | S8 seed work                                               |
| **G8**  | qmd bake + embed-guard                                | **SKIP** — no deployment sets `memory.*`; default backend is `builtin` (`src/config/schema.help.ts:1265`); qmd path never runs (`src/gateway/server-startup-post-attach.ts:155`). Revisit only on a deliberate backend move.                                                                                                                                                       | high       | no                                                             | s8: exclude 2 qmd pieces                                   |
| **G9**  | Revert 4 cast-removal residuals                       | **ADOPT, unconditional** — pure type noise (+9/-18, zero behavior); eliminates guaranteed re-conflicts.                                                                                                                                                                                                                                                                            | high       | no                                                             | S8                                                         |
| **G10** | canvasHostEnabled re-graft                            | **SKIP/DROP** — zero code consumers in either tree; canvas plugin owns enablement (`extensions/canvas/src/config.ts:90-98`); dead duplicate against a legacy hidden key.                                                                                                                                                                                                           | high       | no                                                             | s5: drop one hunk                                          |

---

## 5. Our-defect backlog (seam-dossier cross-map)

Dossier = `nabu-integration/docs/upstream-bump-2026-08-seam-dossier.md`. Note: its silent-regression watchlist has **36** items (`…:493-562`), not 18 — items 19-36 were largely unmapped by earlier passes; the material ones are folded in below.

### 5.1 Fixed or materially changed by the port

| Defect                                                                                                 | Fixed by              | Note                                                                                                                                                                                                                         |
| ------------------------------------------------------------------------------------------------------ | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #1 nabu-files never activates (uploader unregistered)                                                  | **S1**                | same canonical commit as S3; verify at runtime (T3)                                                                                                                                                                          |
| Q3(a)(b) missing contracts.tools / activation — incl. the **live** nabu-email tool refusal             | **S1** (+S7 manifest) | residual: nabu-model-router still un-activated at pin (decision)                                                                                                                                                             |
| Watchlist **#29** async fire-and-forget `session.message` broadcast (our graft; regressed once before) | **S3**                | rewrite to sync pre-projection enrichment; restores ordering invariant O3 — re-verify at the S3 gate                                                                                                                         |
| #5/#6 details.media as sole channel / first-cap destroys media                                         | **S3 partial**        | generated media bypasses tool results entirely (card + `details.nabuFileIds`, cap can't touch it — `src/agents/session-tool-result-guard.ts:482-484`); middleware/tool-result media still capped (browser G5, MEDIA markers) |
| Watchlist **#32** OP_SERVICE_ACCOUNT_TOKEN in gateway process.env (exec-inheritance exposure)          | **S7**                | v3 confines token to op child env; S7 smoke asserts absence                                                                                                                                                                  |
| Q2 deprecated `loadConfig()` (6 sites)                                                                 | **S7 fixes 1/6**      | remaining 5 migrate in the S6 follow-up commit                                                                                                                                                                               |
| #15 nabu-media-upload dead in seeded tenants                                                           | **S1 + S8 manual**    | patch makes it loadable; seed allow/entries edit is manual                                                                                                                                                                   |
| Q14/G7 retired openai-codex ids in seed                                                                | **S8 manual**         | 5-surface chain + runtime pin + `plugins.allow` "openai" add + completions regen                                                                                                                                             |

### 5.2 Unfixed backlog (with insertion points)

**Prominent — shared, live, untouched by any patch:**

1. **#9/Q9 `dm.pair.approve` data loss** — writes a bare array against upstream's `{version:1, allowFrom:[]}` store: approving one id **wipes the whole allowlist**; plus statePath misuse (`src/gateway/server-methods/dm-pairing.ts:73`) and missing account-key filename suffix (:83). Byte-identical at the pin — senw carries the same broken file. Fix: re-express on the upstream pairing store (`src/pairing/pairing-store.ts:714/:551/:574/:372`; shape `src/pairing/allow-from-store-file.ts:15`; dir resolution `:33`); delete the hand-rolled JSON I/O. **Insert: pre-port.**
2. **#3/Q7 `wrapReadFileForHttpUrls`** phantom host-read breaks every local outbound attachment (`src/infra/outbound/message-action-params.ts:434-466`) — shared with pin; S4/S5 _increase_ traffic through it on local-path fallback. Fix: revert to upstream (no caller needs the wrapper; `src/media/web-media.ts:964` handles https); un-reds 2 baseline tests. **Insert: pre-port.**
3. **#2/Q8 deliver section after `SYSTEM_PROMPT_CACHE_BOUNDARY`** (`src/agents/system-prompt.ts:1252` vs :1284) + contradicting red assertion (`src/agents/system-prompt.test.ts:1274` — baseline-confirmed red). Decide: move above boundary + fold into `hashStablePromptInput`, or fix the test. **Insert: pre-port.**
4. **#12/Q11/G4 committed live credentials** + anchored `/.env`. **Insert: pre-port (before s8).**
5. **Q5 residual + D-SEC-1**: unauthenticated `payload.user` + `x-openclaw-session-key` header = full per-user impersonation for any gateway-token holder (RED, both forks). **Insert: ledger entry during S5; fix needs a backend contract decision (JWT+HMAC, bind `(org,user,sessionKey)`).**

**Rest of backlog:** watchlist **#30** SSE history branch discards fileRefs enrichment — **survives at the pin** (`pin:src/gateway/sessions-history-http.ts:218-233`); fix during/after S5 or accept (§10). — **#8** media-uploader singleton dual-load: no diagnostic/test; S3 adds a new core consumer → wiring guard **during S2**. — **Q4/#11** four inline identity derivations → one helper **during S5**. — **#17** `OPENCLAW_ORGANIZATION_ID` fail-open substitution + inconsistent failure shapes across (post-re-add) readers → unify fail-closed **during S6a/S7 adaptation**; add doctor/startup check that the literal `${OPENCLAW_ORGANIZATION_ID}` placeholder never reaches a header. — **Q16** timeout-budget mismatch (10s callers vs 30s×3 uploader ladder; abandoned uploads still mutate uploadCache) → **during S6a**. — **#13/#14/#16/Q13** seed compose volume + skill-token passthroughs, docker-setup.sh resync, bootstrap-marker/doctor → **during S8**. — **#18/#19** model-router `${VAR}` interpolation divergence + duplicated provider literal → **post-port** (router disabled) — but pair with watchlist **#34** (no package.json — correct, T10) and **#35** (string-form model-config hijack) before ever activating it. — **#6 residual** first-cap on middleware media → **post-port**, depends on G5. — Watchlist **#21** nabu-gateway usage-shape drift (hook contract vs `usage.totalTokens/.cost` forwarded verbatim at `extensions/nabu-gateway/index.ts:189`) — S6b edits the file without fixing it; fold in or ticket. — Watchlist **#23** credit gate doesn't cover API-initiated embedded runs — post-S5 the hardened compat ingress gets per-user attribution **and uncapped spend**; ticket with B6-2. — Watchlist **#26/#27/#28** (`as ResponseResource` blinding, `joinPayloadsWithMedia` at 4 sites, fire-and-forget `maybeFinalize`) live in the s5-grafted `openresponses-http.ts` — dropped-hunk hazards; check all 4 join sites after the graft. — **Q18** labeler entries for nabu-\* paths → post-port hygiene. — #4, #7, #10 (descriptor asymmetry), #11 literal fragility: unchanged; remain on the next-upstream-merge seam list. The port does **not** shrink that list except #29/#32.

---

## 6. Backend prerequisites & coordination (va-core-nest)

| ID       | Requirement                                                                                                                                                                                                                                | Current state                                                                                                                                                                                                                 | Gap / action                                                                                                                                                                                                                                                                                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B0**   | WS client speaks protocol v4 (`minProtocol:4/maxProtocol:4`, `client.id` allowed, `client.mode:"backend"`; Ed25519 payload unchanged) before fronting any gateway built from `79369129ce` (`packages/gateway-protocol/src/version.ts:2-6`) | Backend connects to a ~2026.4.x pre-merge image; protocol pin unverified; paired.json pre-seeding deliberately omits `tokens.operator` (required at 6.2 → NOT_PAIRED 1008)                                                    | Bump envelope + switch device seeding **before** their planned `openclaw:local` rebuild from our HEAD (memo `39bb3b29b469`); answer their four 2026-08-04 questions first — **rebuild day otherwise piles a fleet-wide 1002/1008 outage on top of everything** (senw hit exactly this, memos `60141dcedc33`/`f05a08c2dd75`)                                                                                |
| **B5-1** | Ingress sends numeric per-user id in OpenAI `user` on `/v1/responses` + `/v1/chat/completions`                                                                                                                                             | **UNVERIFIED — the freeze switch.** No memo states what the `responsesCompletion` relay puts in `user`; pre-merge uploads never needed it. senw DTO precedent rejects `agent-5`-style ids (memo `de36e891c349`)               | Read `apps/nabu-gateway/src/openclaw/openclaw.service.ts` in va-core-nest; confirm/introduce numeric `app_users.id` **before S5 reaches prod**. Without it: inbound-image persistence, attachment session entries, turn fileRefs and finalText stripping all silently stop while model vision keeps working (masked loss). Also keep `user` consistent with any explicit session-key header (owner split). |
| **B6-1** | files-api accepts `x-user-id` **alongside** `x-organization-id`; per-(org,user) storage+quota; ownership-scoped resolve with per-entry NOT_FOUND; 413 envelope; Idempotency ≤128 org-scoped dedupe                                         | Org dimension shipped since May (memos `d57c93a6bb36`, `ed51737ddc20`); **user dimension absent** — uploads stamp `created_by=0, actor='openclaw'`                                                                            | Net-new backend work; ownership-scoped resolve is the single most important isolation control (fileIds are guessable integers). Required before enabling S6a in prod.                                                                                                                                                                                                                                      |
| **B6-2** | usage-ingest skill-token guard (raw token in header, sha256 at rest, constant-time)                                                                                                                                                        | **NO guard**; route @Public and publicly nginx-proxied, tenant parseInt'd from body — standing HIGH vuln, unfixed since 2026-07-27, fix designed but unscheduled, header name unchosen (memos `4538f76d62f9`, `23572d81e571`) | Ship guard + S6b token **together** (guard alone silently 401s all metering; token alone changes nothing); close the nginx exposure. Sequence with watchlist #23 (compat-ingress credit-gate bypass).                                                                                                                                                                                                      |
| **B6-3** | smtp accepts `{agentId}` + org header; per-org agentId→userId→mailbox; `SMTP_CONFIGURATION_NOT_CONFIGURED`                                                                                                                                 | Org-scoped routes live (org from token hash; one mailbox per org — memo `83c673740073`); no agentId mapping exists                                                                                                            | Backend mapping + error code before enabling S6c.                                                                                                                                                                                                                                                                                                                                                          |
| **B6-4** | (G2) Google scopes drive.readonly + drive.file                                                                                                                                                                                             | Broker exists, composed per (org,channel,user); granted scopes stated nowhere                                                                                                                                                 | Enumerate scopes; decides G2.                                                                                                                                                                                                                                                                                                                                                                              |
| **B7**   | Per-user 1Password broker `(org,userId,channel)`; encrypted per-user tokens; 404/412 semantics; pull-per-invocation                                                                                                                        | Only the v1 per-org `/token` route (two-factor org lookup) exists                                                                                                                                                             | New route + storage; **stop calling `nabu.onepassword.refresh`** (404s the moment S7 lands); retire v1 route; drop `refreshIntervalMs` from patch payloads. Before S7 enable.                                                                                                                                                                                                                              |

**Enablement schedule per surface (prod):** S6a/nabu-files ⇐ B5-1 + B6-1; S6b/ingest ⇐ B6-2 (lockstep); S6c/email ⇐ B6-3; S6d SKILL ⇐ B6-4/G2; S7 ⇐ B7 (+ RPC removal coordinated with the code landing, not the enable); everything ⇐ B0 once the backend rebuilds its image.

**Correspondence summary:** memo `3435bec961cd` (our 047 — the debt) → `20feccf0c82d` (the handoff, acknowledges `5e4394cabed9`, answers 047) → owed-back list §11. Their memo-055 (`f7e9c7640621`) sets the replication-memo bar. Memo-056 pair = B0 contract. Memo-045 (`7c7df1fbc4c3`) = the runtime smoke set (6/6 SSE tests). Memo `4c870400615f` = B7 reference. `d0cf4faf7ec3` is still the **only** `for:nabu-senw`-tagged memo on their server — **no ack of the handoff has been posted yet**. Their four unanswered 2026-08-04 questions (`39bb3b29b469`): paired.json stability vs `gateway.devices.trusted[]`, CIDR operator auto-approve, `gateway.auth.token` rotation surface, image build/labels.

---

## 7. Traps → concrete checks

| Trap                                     | Concrete check / reviewed site                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **T1** cast-removal residue              | `git diff fc6400ede3 79369129ce -- extensions/feishu/src/channel.ts extensions/github-copilot/models.ts extensions/irc/src/setup-core.ts extensions/zai/index.ts` → must show only `as`-cast noise; then G9 revert; post-revert diff vs `fc6400ede3` empty.                                                                                                                                                                                            |
| **T2** typebox split                     | After s6: `grep -rn '@sinclair/typebox' extensions/ package.json` → **0 hits**; `extensions/nabu-email/index.ts:2` still `from "typebox"`. Skip the patch's import hunk. (API-safe: same constructors already compile against typebox 1.1.39; pin root retains bare typebox too.)                                                                                                                                                                      |
| **T3** activation/contracts silent gates | Verify by boot line + `[nabu-files] media uploader + resolver registered` + behavior; NEVER by `openclaw plugins list` (shows contract plugins "disabled" while they fire — `src/plugins/registry.ts:605-613` refuses silently with only a diagnostic).                                                                                                                                                                                                |
| **T4** media-completion dual registry    | `grep -rln "media-completion" src/                                                                                                                                                                                                                                                                                                                                                                                                                     | wc -l`≥ 2 — specifically`src/shared/transcript-only-openclaw-assistant.ts`AND`src/agents/embedded-agent-runner/run/attempt.session-lock.ts:55-59`. Third inline duplicate (`src/agents/embedded-agent-subscribe.handlers.messages.ts:55`) intentionally NOT extended — note or consolidate. |
| **T5** stale tests                       | Exact fixes before the S6 gate: (1) `pin:extensions/nabu-files/src/upload.test.ts:108` → `.rejects.toThrow(/non-numeric userId on upload; refusing/)`; (2) `pin:…:260-261` → `const longUserId = "1".repeat(200);` (numeric, exercises the sha1-capped prefix path). Then `pnpm test extensions/nabu-files` = 13 green (with org env stubs).                                                                                                           |
| **T6** order-of-operations refusals      | Release checklist assertion: no build ships containing the S6a numeric gate (or the s5 fail-closed `batchResolve` partially) without the full S3–S5 threading. Enforce via the stage tags `parity/s3..s5` and atomic s5 apply.                                                                                                                                                                                                                         |
| **T7** no bulk checkouts                 | Policy check in the port PRs: every `git checkout <ref> -- <path>` names a single file from Appendix B; never a directory; never `src/sessions/` or senw's `spawn-seed/` (live secrets at `pin:nabu-integration/spawn-seed/.env:52`, `<redacted>`).                                                                                                                                                                                                    |
| **T8** live config re-read               | Reviewed sites: `pin:extensions/nabu-gateway/index.ts:61-67` (per-event `getLivePluginConfig`), nabu-1password v3 `current()`. Runtime check: rotate a skill token via gateway `config.patch` → next call uses the new token **without restart** (works only through `src/gateway/server-methods/config-write-flow.ts:224` → `src/config/runtime-snapshot.ts:294-343`; direct file writes do NOT propagate — confirm NestJS rotates via the RPC, §10). |
| **T9** plugin outbound HTTPS             | Reviewed pattern: `extensions/nabu-model-router/src/classifier.ts:1,47` (`node:https`). Check: no `fetch(` introduced in any plugin outbound-HTTPS path during the port (global undici dispatcher at `src/infra/net/undici-global-dispatcher.ts` breaks fetch with "other side closed").                                                                                                                                                               |
| **T10** model-router has no package.json | `ls extensions/nabu-model-router/package.json` → absent in both forks — **correct**; extension-iterating scripts skipping it is expected, not a bug to fix.                                                                                                                                                                                                                                                                                            |
| **T11** lockfile                         | Verified: `grep -l 'package.json\|pnpm-lock.yaml'` across all 8 patches = **zero** — the payload never triggers T11. Triggered only if a gate proves a dep missing → `rm pnpm-lock.yaml && pnpm install --lockfile-only`. The T2 adaptation removes the only likely trigger. Big grouped commits: `--no-verify` (oxfmt hook hangs).                                                                                                                    |
| **T12** typecheck lanes                  | Gate command is always all three: `pnpm tsgo && pnpm tsgo:extensions && pnpm check:test-types` (baseline: all green). `pnpm build:docker` is transpile-only — never sufficient.                                                                                                                                                                                                                                                                        |

---

## 8. Baseline (recorded 2026-08-05, `parity/s0`)

From `baseline-results.txt` (initial `pnpm: command not found` failures on two lanes were environment artifacts; corrected reruns included):

| Gate                                               | Result                                                                                                                                                         |
| -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm tsgo:core`                                   | **PASS** (rerun)                                                                                                                                               |
| `pnpm tsgo:extensions`                             | **PASS**                                                                                                                                                       |
| `pnpm check:test-types`                            | **PASS** (rerun)                                                                                                                                               |
| `pnpm plugin-sdk:check-exports`                    | **PASS**                                                                                                                                                       |
| `pnpm plugin-sdk:api:check`                        | **PASS** (`OK docs/.generated/plugin-sdk-api-baseline.sha256`)                                                                                                 |
| `src/agents/system-prompt.test.ts`                 | **1/85 FAIL** — `:1274` deliver-before-boundary (known-red = defect #2/Q8)                                                                                     |
| `src/infra/outbound/message-action-params.test.ts` | **2/32 FAIL** — injected `readFile [Function anonymous]` (known-red = defect #3/Q7 wrapper)                                                                    |
| `src/agents/tool-catalog.test.ts`                  | **1/3 FAIL** — `"deliver"` in coding-profile allow (fork deliver graft vs upstream-shaped assertion; pre-existing fork red)                                    |
| `src/gateway/server-runtime-config.test.ts`        | **4/58 FAIL** (2 tests × 2 projects) — origin-guard reject tests resolve because the guard is commented out (known-red = G3; restoring the guard un-reds them) |
| `extensions/nabu-model-router`                     | **PASS** (25)                                                                                                                                                  |
| `extensions/nabu-media-upload`                     | **PASS** (3)                                                                                                                                                   |

All 8 red tests map 1:1 to pre-existing, documented fork defects — **no unexplained reds**; type/SDK surface is fully green. **PENDING:** docker build + boot-line plugin list before-state (node_modules mid-install; docker/validation lane owns it — record before S1). Post-stage expectation: these reds stay red through the port unless their backlog fixes land (Q7/Q8 pre-port, G3 at S8); any NEW red is port fallout.

---

## 9. Ack-memo draft — **DRAFT ONLY. Do NOT post without operator approval.**

> **tags:** `for:nabu-senw`, `acknowledges:20feccf0c82d`, `answers:20feccf0c82d`, `re:3435bec961cd`
>
> **Receipt + pin confirmation.** Handoff received and byte-verified: all 8 patches applied clean against `79369129ce`; all 65 files byte-identical to `9571e4775f`; re-diff regenerated at 49 commits / 393 files, matching Appendix A/B. `79369129ce` **is truly our tip** — `develop == origin/develop`, tree clean, 0 unpushed commits (only untracked local analysis docs, no code). Your drift since pin (4 docs-only commits) noted; §12 validity holds on our side.
>
> **Memo-047 ledger.** (A) producer-side upload: NOT landed here — zero commits since 2026-06-08; we adopt your S3 (`cdff7f09c4`) as canonical. (Q3) provenance suppression: NOT landed; your s3 wake-hiding hunk is the fix. (Q4) assistant-message `nabuFileIds` binding: NOT landed; your media-completion card implements our verdict. (Q1) wait-primitive verification we owed: never run; your hold-open-is-DOA finding accepted — item closed.
>
> **Gate answers (evidence-decided today):** G9 adopt (reverting the 4 cast files). G8 skip (we run builtin memory everywhere; no `memory.*` in any deployment). G10 skip — and flagging back: `canvasHostEnabled` has zero code consumers at your pin too; consider dropping it your side. G7 adopt (we use ChatGPT OAuth; migrating our seed per your chain + `agentRuntime.id:"openclaw"` pin). **PENDING operator:** G2 (enumerating our granted Google scopes; note your own `google-workspace-nestjs-implementation.md` at the pin still marks drive.readonly Phase-2), G3 (adopt-leaning; prod origin list needed), G5, G6 (adopt-leaning for seed; live-instance audit needed).
>
> **G4 rotation status:** PENDING — operator decision outstanding on rotate-and-keep vs history-scrub; until resolved we treat everything in both trees as burned per your §7.
>
> **Backend timeline (composed org,user):** B6-1 user dimension, B6-2 ingest guard, B6-3 agentId mapping, B7 per-user broker — currently unscheduled on va-core-nest; B5-1 (`user` field) unverified; B0 protocol-v4 bump will be sequenced before our image rebuild. Dates to follow in a supersedes memo once scheduled. Coordination ask: your S7 removal of `nabu.onepassword.refresh` will 404 our backend's rotation caller — we'll land B7 changes in lockstep.
>
> **Pin defects observed while porting (FYI):** dangling package.json scripts at your pin (`canon:*`, `stage:bundled-plugin-runtime-deps` — backing files absent); T5's two stale upload.test.ts assertions confirmed; SPEC.md:930-936 claims an agents.files.set allowlist expansion that is absent at your pin (`pin:src/gateway/server-methods/agents.ts:64-105` is upstream-shaped — your provisioner may be affected); dossier-#30 SSE history enrichment discard still present at the pin; nabu-model-router still has no activation block.
>
> **Owed next:** post-port replication memo at your memo-055 bar (conflict counts, gates run, boot verification), tagged `for:nabu-senw`.

---

## 10. Open questions for the operator

1. **G4 (pre-port, blocking hygiene):** rotate+untrack+ignore-fix vs full history scrub for `nabu-integration/spawn-seed/**` secrets (env keys, provider apiKeys, OAuth material len 1960/90, device privateKeyPem — all `<redacted>`, all burned)? Rotation was declined 2026-07-22 (memo `23572d81e571`) — re-decide before s8 touches `.gitignore`.
2. **G2:** do we grant `drive.readonly` today (backend/Google console — not answerable from this repo)? If Phase-1 only, s6d SKILL/tool-string are held. Note the pin's own backend doc contradicts its SKILL (`pin:nabu-integration/docs/google-workspace-nestjs-implementation.md:311`).
3. **G5:** auto-deliver browser screenshots on the vision-success path (extending existing no-vision/failure behavior) to all channels? If yes, also allow-list nabu-media-upload for the web path and confirm the #3 fix lands first (local-path fallback rides it).
4. **G3:** approve restoring the origin guard, and supply the real browser-facing `gateway.controlUi.allowedOrigins` per deployment (seed's loopback-only entry under `bind=lan` is almost certainly wrong at request time).
5. **Post-S5 owner-less sessions:** accept permanently-empty Files-API fileRefs for operator `agent:<id>:main` webchat/group/cron sessions (senw's design), or adapt (`chat-file-refs.ts` org-scoped fallthrough, or extend owner derivation)? This is a real regression for operator webchat if we rely on it.
6. **D-SEC-1 ownership + timing:** who owns the `x-openclaw-session-key` fix (operator-JWT allowed-userIds issued by va-core-nest + HMAC bind, over `(org,user,sessionKey)`) and does it land before or after prod files-api? Related: is bearer-token ⇒ `senderIsOwner=true` acceptable when the bearer is held by a multi-user backend?
7. **B5-1 verification:** commission the va-core-nest read (`apps/nabu-gateway/src/openclaw/openclaw.service.ts` `user` field) — the single fact deciding whether S5 can ever ship to prod; also pin the ingress rule keeping `user` and session-key owner consistent.
8. **B0 sequencing:** who answers the backend's four 2026-08-04 questions (memo `39bb3b29b469`) before their image rebuild from our HEAD?
9. **Token rotation path (T8/Q2):** confirm NestJS rotates skill tokens exclusively via gateway `config.patch` RPC — direct `openclaw.json` writes silently freeze tokens and the credit kill switch at startup values.
10. **S6 detail decisions:** (a) nabu-email org header fail-closed (throw like nabu-files) or best-effort? (recommend fail-closed — B6-3 requires it); (b) enforce numeric gate on resolve too, or keep senw's truthy asymmetry?; (c) fix dossier #30 (SSE history) ourselves or accept?; (d) add regression tests for the fail-closed gates senw shipped untested?
11. **Idempotency/quota composition (B6-1 detail):** dedupe key `(org, key)` or `(org, user, key)`; MinIO layout org segment vs per-org buckets; is numeric userId globally unique or per-org (`agent-<userId>` collides across per-org stacks otherwise)?
12. **G6 rollout:** adopt baseline+validator for the seed now (recommended, dormant); when do existing instances get audited, and when does `session.dmScope` flip from `main` (senw deferred to channel go-live)?
13. **nabu-model-router:** activate (then also fix watchlist #34/#35) or explicitly disable in seed like senw and record the decision?
14. **G7 execution:** which dedicated ChatGPT identity does OUR prod use (single live session; separate account — not another seat — if local+prod run OAuth simultaneously)?
15. **Purge/RMQ divergences (FE/backend contracts):** does purge become org-admin-scoped (senw's is platform-wide by design), and do we keep our RMQ `FILE_UPLOADED` event senw dropped (we have RMQ; they don't)? One written org-carriage rule per hop (JWT claim vs header) is still needed for FE routes.
16. **Ack memo:** approve posting §9 (and whether to disclose the declined-rotation status verbatim), plus reporting the pin defects listed there back to senw.

---

_Every claim above carries its source cohort's file:line citations, verified against `79369129ce` (ours) and `9571e4775f` (pin) on 2026-08-04/05; cohort-verdict corrections have been applied (corrected line anchors, the canvas-runtime and G5 delivery-path reframings, the 6-entry/13-site org ledger count, the 36-item watchlist scope, and the nabu-1password seed-entry state). Read-only analysis — no repo files were modified._
