<!-- SENW→VA parity-port PLAN. Composed 2026-08-07 from the execution dossier
     (senw-parity-execution-dossier.md — the fact base; every claim there is cited and verified)
     plus four operator decisions taken 2026-08-07. The playbook in ~/Desktop/va-parity-handover/
     remains the upstream authority; this plan is our ordered execution of it. -->

# SENW parity port — execution plan

Pins: ours `79369129ce` · senw `9571e4775f` · base `fc6400ede3`. Payload byte-verified 65/65.
Baseline recorded (dossier §8): tsgo/SDK all green; 8 known-red tests in 4 files, all mapped.

**Operator decisions baked in (2026-08-07):**

- Pre-port fixes: **all three** (dm.pair store rewrite, wrapReadFile revert, cache-boundary fix).
- G4: **untrack + remove** seed credential files; most values already revoked — verify list below; **no history scrub** (pins stay valid).
- G5: **adopt** browser vision-success delivery (keep the both-sides rationale comment).
- G3: **restore** the Control-UI origin guard at S8 (prod origin list needed before deploy, not before code).
- Previously evidence-decided: G7 adopt · G8 skip · G9 adopt · G10 drop. G2 **held** (s6d SKILL subset excluded until backend scopes are enumerated).

**Standing rules (playbook §2/§9, dossier §1.3/§7):** commit per stage, `--no-verify` on big diffs; tag `parity/s<N>` after each green gate; gate = all 3 tsgo lanes minimum; s5 applies **atomically**; nothing ships to prod between S3 and S5; no build ships with the S6a numeric gate but without S3–S5 threading; if either fork merges upstream mid-port, STOP — playbook void for conflicted surfaces.

---

## Phase 0 — branch + baseline close-out

1. `git checkout -b feat/senw-parity 79369129ce`; tag `parity/s0-baseline`.
2. Record the one PENDING baseline item: `DOCKER_BUILDKIT=1 docker build -t openclaw:local .` + boot-line plugin list (before-state for T3 comparisons).

## Phase 1 — pre-port fixes (4 commits, ours; also our contribution back to senw)

**P1a — G4 secrets (first, before anything touches .gitignore):**

- `git rm --cached` + delete from tree: `nabu-integration/spawn-seed/.env`, `.openclaw/agents/main/agent/auth-profiles.json`, `.openclaw/agents/main/agent/models.json`, `.openclaw/identity/device.json`, `.openclaw/logs/config-audit.jsonl`, `.openclaw/update-check.json` (and the four `completions/*` files — junk, not secrets). Replace `.env` with a `.env.example` (names only).
- Neutralize the literal `gateway.auth.token` inside the tracked seed `openclaw.json` (replace with `${OPENCLAW_GATEWAY_TOKEN}` placeholder).
- Fix ignore rules: restore upstream's unanchored `.env` (our `/.env` anchoring at `.gitignore:3` is why the seed env got tracked — dossier #12) + explicit ignores for the seed credential paths.
- **Operator verify (name-only checklist, values stay unprinted):** confirm revoked — seed gateway token (64ch) · `CLOUDFLARE_AI_GATEWAY_API_KEY` · `MINIMAX_API_KEY` · the CF + MiniMax keys in auth-profiles (40/126ch) · OAuth access/refresh material in auth-profiles (1960/90ch) · provider apiKeys in models.json · `identity/device.json` privateKeyPem. Anything not yet revoked gets rotated now. History retains the burned values by design (no scrub → pins and byte-verify stay valid).

**P1b — dm.pair.approve data-loss (#9/Q9):** re-express `src/gateway/server-methods/dm-pairing.ts` on the upstream pairing store (`src/pairing/pairing-store.ts:714/:551/:574`; shape `src/pairing/allow-from-store-file.ts:15`; dir resolution `:33`) — deletes the hand-rolled JSON I/O, the bare-array write that wipes allowlists, the `statePath` misuse (`:73`), and the missing account-key suffix (`:83`). Add a regression test: approve-on-populated-store preserves prior entries. senw carries the identical broken file — this goes in the ack memo.

**P1c — wrapReadFileForHttpUrls revert (#3/Q7):** revert `src/infra/outbound/message-action-params.ts:434-466` to upstream (no caller needs it; `src/media/web-media.ts:964` already handles https). Un-reds 2 baseline tests. Must precede S4/S5 (they increase traffic through this path) and G5 (fallback rides it).

**P1d — deliver-section cache boundary (#2/Q8):** move `buildDeliverSection` above `SYSTEM_PROMPT_CACHE_BOUNDARY` (`src/agents/system-prompt.ts:1252` vs `:1284`) and fold into `hashStablePromptInput` (`:981`). One-time provider prompt-cache invalidation — expected, note in commit. Un-reds 1. Same commit: update the `tool-catalog.test.ts:27` expectation to include our shipped `deliver` tool (test-only, un-reds 1).

**Gate:** 3 tsgo lanes + the 4 formerly-red files → only the 4 origin-guard reds remain (they fall at S8/G3). Tag `parity/pre-port`.

## Phase 2 — the eight stages

| #      | Apply                                                                                          | ADAPT (dossier §3 ledger — the authority for exact edits)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Gate beyond tsgo×3                                                                                                                                                                                           |
| ------ | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **S1** | `s1-manifests.patch` verbatim                                                                  | none now; description rewording lands with S6 (same-commit rule)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | docker build; boot line lists the 5 plugins; T3: verify by boot line + behavior, never `plugins list`                                                                                                        |
| **S2** | `s2-sdk-identity.patch`                                                                        | no-op guard: `SessionOwner` stays `{userId}` (org is env-level). Manual: `pnpm plugin-sdk:api:gen` (never hand-copy), restore `docs/plugins/sdk-overview.md` rows, add the `parseSessionOwner` CI guard script, add the media-uploader/resolver **singleton wiring guard** (#8)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `pnpm test src/sessions/session-key-utils.test.ts` + SDK contract trio                                                                                                                                       |
| **S3** | `s3-detached-media.patch`                                                                      | none (pure senw fix we adopt)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | T4: `grep -rln media-completion src/ \| wc -l` ≥ 2; local-mockable smoke now, full image_generate smoke deferred to staging                                                                                  |
| **S4** | `s4-threading-browser.patch` **including** the browser hunk (G5=adopt; keep rationale comment) | middleware `(event,ctx)` verified compatible                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `pnpm test extensions/nabu-media-upload`                                                                                                                                                                     |
| **S5** | `s5-ingress-identity.patch` **atomically**, minus the `canvasHostEnabled` hunk (G10=drop)      | fail-closed `batchResolve` as-is; consolidate the 4 inline `user→senderId` derivations into one helper (Q4/#11); fix #30 (SSE history branch discards enrichment — small, ours); create `nabu-integration/tech-debt.md` with the **D-SEC-1** entry (`x-openclaw-session-key` impersonation, RED, both forks)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | gateway test files touched; **code lands, prod enable stays blocked on B5-1**                                                                                                                                |
| **S6** | `s6-plugin-contracts.patch`                                                                    | the big ADAPT commit: 8 org re-adds (upload.ts ×4, resolve.ts ×4 sites — dossier §3 has exact lines), test env-stubs + `x-organization-id` assertions, **T2** skip the typebox import hunk (keep bare `typebox`), **T5** fix the 2 stale upload.test.ts assertions, org-env **fail-closed** unification (#17) incl. the placeholder-never-reaches-header check, timeout-budget fix (Q16), `loadConfig→current()` migration (5 remaining sites, Q2), S1 description rewording to composed wording, nabu-email org header **fail-closed** (default; flip if operator overrides §10-Q10a). **s6d SKILL/tool-string hunks HELD** (G2) — apply `index.ts` comment hunk only                                                                                                                                                         | `pnpm test extensions/nabu-files` = 13 green; nabu-model-router suite                                                                                                                                        |
| **S7** | `s7-1password.patch` (12 plugin files + 3 root-skill deletions)                                | org header re-add in `token.ts` (fail-closed), interface/manifest wording per ledger                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `pnpm test extensions/nabu-1password`; smoke: `OP_SERVICE_ACCOUNT_TOKEN` absent from gateway `process.env`; **coordinate: backend's `nabu.onepassword.refresh` caller 404s when this deploys — B7 lockstep** |
| **S8** | `s8-build-deploy.patch` hand-merged                                                            | Dockerfile: keep our store-seed resilience + insert their prebuilt filter, **exclude qmd pieces** (G8); `.gitignore`: adopt instances/IDENTITY hunks, skip `knowledge/`; skip `.npmrc` + `pnpm-workspace.yaml` hunks; **G9**: `git checkout main --` the 4 cast files; **G3**: restore the origin guard (un-reds the last 4 baseline tests) + seed `allowedOrigins` placeholder; **G7 chain**: seed `openai-codex→openai` migration (5 surfaces + `agentRuntime.id:"openclaw"` pin + completions regen); seed fixes: add `nabu-media-upload` + `nabu-1password` to plugins.allow/entries (#15), auth-profile-secret-dir compose mount (#13), `NABU_ONE_PASSWORD_SKILL_TOKEN` passthrough rename, docker-setup.sh resync from upstream (#14); G6 validator ported (dormant, seed-only); labeler entries for nabu-\* paths (Q18) | full §10 run: 3 lanes + SDK trio + docker build + boot + `plugins list \| grep nabu` + validator dry-run                                                                                                     |

## Phase 3 — close-out

1. Full verification: all gates green; known-red count = **0** (all 8 baseline reds resolved by P1c/P1d/S8-G3/expectation fix).
2. Runtime smoke set (memo-045 bar) against dev backend for every surface whose B-prereq exists; explicitly list which smokes are deferred and why.
3. Update the seam dossier's org-touchpoint inventory (ledger asks to keep it in sync).
4. Ack memo (dossier §9, updated with: G4 = "revoked + untracked, no scrub", G5 adopt, G3 restore, pre-port fixes as our give-back incl. the dm.pair fix senw also needs) — **posted only after you approve the final text**.
5. Replication memo at their memo-055 bar. Tag `parity/s8-complete`. Land per your call (PR vs direct).

## Prod enablement — separate track, NOT part of code landing

| Surface                  | Blocked on                                                                                              | Status                                |
| ------------------------ | ------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| S5 ingress in prod       | **B5-1** — verify `apps/nabu-gateway/src/openclaw/openclaw.service.ts` sends numeric `user`             | unverified — the single deciding fact |
| S6a files upload/resolve | B5-1 + B6-1 (backend x-user-id + ownership-scoped resolve)                                              | backend work unscheduled              |
| S6b usage ingest         | B6-2 **in lockstep** (guard + token together; route is publicly exposed today — standing HIGH)          | fix designed, unscheduled             |
| S6c email                | B6-3 (agentId→mailbox mapping)                                                                          | unscheduled                           |
| S6d SKILL                | B6-4 / G2 scope enumeration                                                                             | held                                  |
| S7 1password             | B7 (per-user broker route) + retire v1 refresh caller                                                   | unscheduled                           |
| Everything               | B0 — backend answers its four 2026-08-04 questions **before** rebuilding from our HEAD (protocol v3→v4) | outstanding                           |

## Open items riding as defaults (flag on change)

Q5 owner-less sessions: accept senw's design for now (empty fileRefs for operator sessions), revisit after S5. · Q13 model-router: stays config-disabled like senw; #34/#35 fixed before any activation. · Q11 idempotency/quota composition + Q15 purge/RMQ: backend-contract questions, folded into B6-1/B6-3 scheduling. · Q14 ChatGPT prod identity: needed at G7 seed execution, not before. · D-SEC-1 ownership: tracked in the new tech-debt ledger, fix needs a backend contract decision.
