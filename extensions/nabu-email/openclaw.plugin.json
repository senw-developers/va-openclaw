{
  "id": "nabu-email",
  "activation": {
    "onStartup": true
  },
  "name": "@va-team/nabu-email",
  "description": "Send and fetch emails via the VA.Team backend API",
  "skills": ["./skills"],
  "contracts": {
    "tools": ["nabu_email_send", "nabu_email_fetch"]
  },
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiToken": {
        "type": "string",
        "description": "Platform skill token issued by senw-core; authenticates the gateway↔senw-core channel (channel auth only — per-user mailbox routing is by `agentId` from trusted tool context). See SPEC.md D16."
      },
      "apiBaseUrl": {
        "type": "string",
        "description": "NestJS backend base URL — Docker-internal"
      }
    }
  },
  "uiHints": {
    "apiToken": {
      "label": "API Token",
      "sensitive": true
    },
    "apiBaseUrl": {
      "label": "API Base URL",
      "placeholder": "http://app:6001"
    }
  }
}
