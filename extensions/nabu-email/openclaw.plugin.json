{
  "id": "nabu-email",
  "activation": {
    "onStartup": true
  },
  "name": "@va-team/nabu-email",
  "description": "Send and fetch emails via the VA.Team backend API",
  "skills": ["./skills"],
  "contracts": {
    "tools": ["nabu_email_send", "nabu_email_fetch"]
  },
  "enabledByDefault": true,
  "configContracts": {
    "secretInputs": {
      "paths": [
        {
          "path": "apiToken",
          "expected": "string"
        }
      ]
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiToken": {
        "type": ["string", "object"],
        "description": "Per-organization skill token issued by the backend SMTP module; authenticates the gateway↔backend channel only. Requests carry x-organization-id (env-derived); per-user mailbox routing is by agentId from trusted tool context, never from the tool schema. Accepts either a literal string or a SecretRef object {source, provider, id} resolved from env."
      },
      "apiBaseUrl": {
        "type": "string",
        "description": "NestJS backend base URL — Docker-internal"
      }
    }
  },
  "uiHints": {
    "apiToken": {
      "label": "API Token",
      "sensitive": true
    },
    "apiBaseUrl": {
      "label": "API Base URL",
      "placeholder": "http://app:6001"
    }
  }
}
