{
  "id": "nabu-gateway",
  "activation": {
    "onStartup": true
  },
  "name": "@va-team/nabu-gateway",
  "description": "VA.Team gateway bridge — LLM usage tracking via Cloudflare AI Gateway and credit-based gating",
  "enabledByDefault": true,
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Whether LLM calls are allowed — toggled by NestJS when credits are exhausted/replenished",
        "default": true
      },
      "apiBaseUrl": {
        "type": "string",
        "description": "NestJS backend base URL — Docker-internal",
        "default": "http://app:6001"
      },
      "apiToken": {
        "type": "string",
        "description": "Platform skill token issued by senw-core; sent as x-skill-token on usage/ingest (channel auth). Same token as nabu-email. See SPEC.md D16."
      }
    }
  },
  "uiHints": {
    "apiBaseUrl": {
      "label": "API Base URL",
      "placeholder": "http://app:6001"
    },
    "apiToken": {
      "label": "API Token",
      "sensitive": true
    }
  }
}
