---
summary: "Per-tenant Google Drive + Calendar access. Vends short-lived OAuth access tokens fetched from the Nabu backend and exposes a single passthrough tool the agent uses against Google REST APIs."
read_when:
  - You are installing, configuring, or auditing the nabu-google-workspace plugin
title: "Nabu Google Workspace plugin"
---

# Nabu Google Workspace plugin

Per-tenant Google Drive + Calendar access. Vends short-lived OAuth access tokens fetched from the Nabu backend and exposes a single passthrough tool the agent uses against Google REST APIs.

## Distribution

- Package: `@va-team/nabu-google-workspace`
- Install route: included in OpenClaw

## Surface

contracts: tools; skills
